# Base URL for the FastAPI server that exposes the Grocy routes.
# Set to the local dev server (e.g., http://localhost:8000) or the
# deployed API endpoint that the dashboard should call.
KANOMNOM_API_BASE_URL=

# Optional: expose the same value to client-side code. Only set this if the
# FastAPI server is safe to call directly from the browser.
# NEXT_PUBLIC_API_BASE_URL=
